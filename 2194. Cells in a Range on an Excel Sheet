class Solution {
public:
    vector<string> cellsInRange(string s) {
        vector<string> ans;
        char ch=s[3],c=s[4];
        for(char i=s[0];i<=ch;i++){
            for(char j=s[1];j<=c;j++){
                ans.push_back({i,j});
            }
        }


        return ans;
    }
};
