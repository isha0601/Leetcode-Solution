class Solution {
public:
    vector<int> getConcatenation(vector<int>& nums) {
        vector<int> answer1(2*nums.size());
        for(int i=0;i<nums.size();i++){
            answer1[i]=nums[i];
            answer1[nums.size()+i]=nums[i];
        }
        return answer1;
    }
};
