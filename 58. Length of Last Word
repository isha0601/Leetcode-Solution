class Solution {
public:
    int lengthOfLastWord(string s) {
        vector<string> v;
        stringstream ss(s);
        string word;
        while(ss>>word){
            v.push_back(word);
        }
        return v[v.size()-1].length();
    }
};
