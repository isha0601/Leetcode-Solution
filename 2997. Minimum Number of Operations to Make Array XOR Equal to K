class Solution {
public:
    int minOperations(vector<int>& nums, int k) {
        int xorTotal=0;
        for(int i=0;i<nums.size();i++){
            xorTotal^=nums[i];
       }
       return __builtin_popcount(xorTotal^k);
    }
};
