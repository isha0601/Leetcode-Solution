class Solution {
public:
    int minimumOperations(vector<int>& nums) {
        
        unordered_set<int> set(nums.begin(),nums.end());


        return set.count(0) ? set.size()-1:set.size();
    }
};
