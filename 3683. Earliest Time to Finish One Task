class Solution {
public:
    int earliestTime(vector<vector<int>>& tasks) {
        int mini=INT_MAX;
        for(auto &p: tasks){
            int sum=p[0]+p[1];
            mini=min(sum,mini);
        }
        return mini;
    }
};
