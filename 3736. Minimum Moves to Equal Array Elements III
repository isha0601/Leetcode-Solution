class Solution {
public:
    int minMoves(vector<int>& nums) {
        int maxi=*max_element(nums.begin(),nums.end());
        int count=0;
        for(int val:nums){
            int num=abs(maxi-val);
            count+=num;
        }
        return count;
    }
};
