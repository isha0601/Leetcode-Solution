class Solution {
public:
    int countPrefixes(vector<string>& words, string s) {
        int ans=0;
        for(string str: words){
            if(s.find(str)==0)ans++;
        }
        return ans;
    }
};
